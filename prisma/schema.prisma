generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Link {
  id        String   @id
  target    String
  createdAt DateTime @default(now()) @db.Timestamp(6)
}
